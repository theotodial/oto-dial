# Install into /etc/nginx/conf.d/ (included from http{} on Ubuntu) BEFORE site configs.
# Required for correct WebSocket / long-polling when mixing normal HTTP and Upgrade traffic.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
